namespace Refactoring
{
    // 피격 시 대상에게 적용할 효과의 공통 표식.
    // 히트박스는 효과들을 담아 전달만 하고, 실제 처리(넉백·상태이상 등)는 받는 쪽이 타입을 보고 한다.
    // 새 효과는 이 인터페이스를 구현한 타입을 추가하기만 하면 된다(기존 코드 수정 없음).
    public interface IHitReaction { }
}
