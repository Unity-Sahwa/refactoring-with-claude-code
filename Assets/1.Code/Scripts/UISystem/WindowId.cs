namespace Refactoring
{
    // 창 종류를 구분하는 이름표. 창을 글자가 아니라 이 값으로 지목한다.
    public enum WindowId
    {
        MainMenu,
        Pause,
        NewGameConfirm,
        LoadSlots,
        LoadConfirm,
        Settings,
        SettingsGameplay,
        SettingsSound,
        SettingsLanguage,
        QuitConfirm,
        Credit,
        StoryToon,
    }
}
